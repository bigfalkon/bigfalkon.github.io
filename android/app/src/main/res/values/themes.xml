<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- UI tamamen Compose; bu tema yalnızca pencere/sistem çubuğu renkleri için. -->
    <style name="Theme.KarakterEvreni" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:colorBackground">@color/bg</item>
    </style>

    <style name="Theme.KarakterEvreni.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.KarakterEvreni</item>
    </style>
</resources>
